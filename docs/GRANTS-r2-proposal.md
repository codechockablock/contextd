# Delegation grants r2 — PROPOSAL (canonical copy: master)

This lane's copy is retired. Read the live document with:

    git show master:docs/GRANTS-r2-proposal.md

The full text hand-mirrored here (b28aa25; recover it with
`git show b28aa25:docs/GRANTS-r2-proposal.md`) was a snapshot of the
2026-08-15 master-side session, and its load-bearing claims were true of
master's tree, not this one: the authority plane (`native/`,
`ctx security`, the signature-verifying reduction), the calibration
mission (docs/GRANT_CALIBRATION.md, experiments/grant_calibration/) and
the enrollment runbook all live on master — this branch carries none of
that code, so a snapshot presenting its facts as "verified against the
ledger and the working tree" misdescribed the tree it sat in. Its state
lines were also overtaken the same evening: the canonical map records
the production signer enrolled and the field window running as of
2026-08-15. Master's operating map has since retired hand-mirroring
session docs onto lanes for exactly this failure — divergent copies
drift. This stub is the drift-proof form: it asserts nothing that can
go stale, only where the truth lives.
